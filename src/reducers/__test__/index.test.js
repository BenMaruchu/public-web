import reducer from '../index';
import selectMapPoint from '../../actions';

describe('selectMapPoint reducer', () => {
  it('should return the initialState', () => {
    // const initialState = {};
    // const action = {};
    // const state = reducer(undefined, action);
    expect(1).toEqual(1);
  });
});
